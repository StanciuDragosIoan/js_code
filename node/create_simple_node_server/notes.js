/*
    Node.js program lifecycle 

    the event loop: a loop process managed by node which keeps on running as long as there is
    'work' to do (as long as there are event listeners registered)

    e.g. the request listener function passed to createServer()
*/